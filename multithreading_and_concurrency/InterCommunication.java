package multithreading_and_concurrency;

/**
 * @note There are three ways for the threads to communicate with each other.
 * @note all threads in the same program share the same memory space.
 * @first through commonly shared data
 * @second using thread control methods - suspend(), resume(), join()
 * @third by using three methods - wait(), notify(), notifyAll()
 */
public class InterCommunication {

}
